{
  "name": "ani-bot",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --env-file=.env --watch src/index.ts",
    "deploy:commands": "bun run --env-file=.env src/deploy-commands.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "bufferutil": "^4.0.8",
    "discord.js": "^14.15.3",
    "utf-8-validate": "^6.0.4"
  }
}
